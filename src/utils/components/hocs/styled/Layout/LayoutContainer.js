import styled from 'styled-components';

//TODO: 
export default (Component) => {
    return styled(Component) `
        margin: auto;
        /*TODO: Make this a media query*/
        width: 900px !important;
        position: relative;
    `
}
